{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
{{- $pages := .Pages -}}
{{- if .IsHome -}}
  {{/* 🚀 明确指定只抓取 posts(长文) 和 laodao(唠叨) 两个分区 */}}
  {{- $pages = where site.RegularPages "Section" "in" (slice "posts" "laodao") -}}
{{- end -}}
{{- $pages = where $pages "Params.hidden" "!=" true -}}

{{- $limit := site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ site.Params.description }}</description>
    
    {{- with site.Params.logo }}
    <image>
      <url>{{ .icon | absURL }}</url>
      <link>{{ .icon | absURL }}</link>
    </image>
    {{- end }}
    
    <generator>Hugo -- gohugo.io</generator>
    {{- with site.LanguageCode }}<language>{{.}}</language>{{end}}
    
    {{- with site.Params.author.email }}
    <managingEditor>{{.}}{{ with site.Params.author.name }} ({{.}}){{end}}</managingEditor>
    <webMaster>{{.}}{{ with site.Params.author.name }} ({{.}}){{end}}</webMaster>
    {{- end }}
    
    {{- with site.Copyright }}<copyright>{{.}}</copyright>{{end}}
    {{- if not .Date.IsZero }}<lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    
    {{- with .OutputFormats.Get "RSS" -}}
      {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    
    {{ range $pages }}
      {{- if and (ne .Layout `search`) (ne .Layout `archives`) }}
      <item>
        {{- $isLaodao := eq .Section "laodao" -}}

        <title>{{ if $isLaodao }}{{ .Date.Format "2006-01-02 15:04" }}{{ else }}{{ .Title }}{{ end }}</title>
        <link>{{ .Permalink }}</link>
        <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
        {{- with site.Params.author.email }}<author>{{.}}{{ with site.Params.author.name }} ({{.}}){{end}}</author>{{end}}
        <guid>{{ .Permalink }}</guid>
        
        {{/* 1. 智能提取首图 */}}
        {{- $coverImage := "" -}}
        {{- if .Params.image -}}
          {{- $coverImage = printf `<img src="%s" alt="%s" style="max-width:100%%;" />` (.Params.image | absURL) .Title -}}
        {{- else if $isLaodao -}}
          {{- $imgs := findRE "<img[^>]+>" .Content 1 -}}
          {{- if len $imgs -}}
            {{- $coverImage = index $imgs 0 -}}
          {{- end -}}
        {{- end -}}

        {{/* 2. 强力摘要 */}}
        {{- $descText := "" -}}
        {{- if $isLaodao -}}
          {{- $descText = .Plain | truncate 100 -}}
        {{- else -}}
          {{- $descText = .Description | default .Summary -}}
        {{- end -}}
        
        <description>{{ printf "<![CDATA[%s<p>%s</p>]]>" $coverImage $descText | safeHTML }}</description>
        
        {{- if not site.Params.RSSNoContent}}
          {{/* 3. 渲染正文 */}}
          {{- $contentImage := "" -}}
          {{- if and (not $isLaodao) .Params.image -}}
            {{- $contentImage = $coverImage -}}
          {{- end -}}
          <content:encoded>{{ (printf "<![CDATA[%s%s]]>" $contentImage .Content) | safeHTML }}</content:encoded>
        {{- end }}
      </item>
      {{- end }}
    {{ end }}

    <follow_challenge>
      <feedId>52982633250295857</feedId>
      <userId>58707550957768704</userId>
    </follow_challenge>

  </channel>
</rss>